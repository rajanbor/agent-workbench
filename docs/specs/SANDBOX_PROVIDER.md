# Spec: sandbox provider

Acceptance: a provider reports capabilities and effective policy; native-user and container providers satisfy the same lifecycle API; unsupported restrictions return an explicit error; a run cannot start without an approved workspace scope.
