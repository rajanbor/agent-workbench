# Spec: agent runtime

Acceptance: provider-neutral runs stream normalized events; commands are idempotent; permissions and budget are checked before tools; approvals block execution until explicit resolution; no provider password is stored by the application.
