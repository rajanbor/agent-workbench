[package]
name = "open-cube-engine"
version = "0.2.0-alpha.1"
edition = "2021"
license = "MIT"
description = "Portable engine types for Open Cube desktop clients"

[dependencies]
serde = { version = "1", features = ["derive"] }
