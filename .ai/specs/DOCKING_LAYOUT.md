# Spec: docking layout

Acceptance: a session may contain panel groups and tabs; users can split right/down, move, close and maximize panels; layout persists locally; restoration handles missing panel types safely.
