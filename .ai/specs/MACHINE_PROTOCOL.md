# Spec: machine protocol

Acceptance: local daemon emits capability snapshots and event cursors; remote peers require pairing and encrypted transport; scheduling records the selected machine; clients can reconnect with snapshot plus replay.
